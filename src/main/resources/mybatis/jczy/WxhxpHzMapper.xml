<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.smart119.jczy.dao.WxhxpHzDao">

	<!-- 可根据自己的需求，是否要使用 -->
	<resultMap type="com.smart119.jczy.domain.WxhxpHzDO" id="wxhxpHzMap">
					<result property="id" column="id"/>
					<result property="cnName" column="cn_name"/>
					<result property="content" column="content"/>
			</resultMap>
    <select id="list" resultType="com.smart119.jczy.domain.WxhxpHzDO">
		select id,cn_name,content from jczy_wxhxp_hz
		<where>
			1=1
		<if test="searchName != null and searchName != ''">and cn_name like '%${searchName}%'</if>
		</where>
		<choose>
			<when test="sort != null and sort.trim() != ''">
				order by ${sort} ${order}
			</when>
		</choose>
		<if test="offset != null and limit != null">
			limit #{offset}, #{limit}
		</if>
	</select>

	<select id="count" resultType="java.lang.Integer">
		select count(*) from jczy_wxhxp_hz
		<where>
			1=1
			<if test="searchName != null and searchName != ''">and cn_name like '%${searchName}%'</if>
		</where>
	</select>
	<select id="get" resultType="com.smart119.jczy.domain.WxhxpHzDO">
		select id,cn_name,content from jczy_wxhxp_hz  where id = #{value}
	</select>

	<insert id="save" parameterType="com.smart119.jczy.domain.WxhxpHzDO" useGeneratedKeys="true" keyProperty="id">
		insert into jczy_wxhxp_hz
		(
		`id`,
		`cn_name`,
		`content`
		)
		values
		(
		#{id},
		#{cnName},
		#{content}
		)
	</insert>

	<update id="update" parameterType="com.smart119.jczy.domain.WxhxpHzDO">
		update jczy_wxhxp_hz
		<set>
			<if test="cnName != null">`cn_name` = #{cnName},</if>
			<if test="content != null">`content` = #{content}</if>
		</set>
		where id = #{id}
	</update>

	<delete id="remove">
		delete from jczy_wxhxp_hz where id = #{id}
	</delete>

	<delete id="batchRemove">
		delete from jczy_wxhxp_hz where id in
		<foreach item="id" collection="array" open="(" separator="," close=")">
			#{id}
		</foreach>
	</delete>

</mapper>